import { UserSearchPipePipe } from './user-search-pipe.pipe';

describe('UserSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
